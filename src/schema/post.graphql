# scalar Date

type Query {
	getPosts: [Post!]
	getPost(postId: ID!): Post!
}

type Post {
	id: ID!
	body: String!
	createdAt: String!
	username: String!
}

type Mutation {
	createPost(body: String!): Post!
	deletePost(postId: ID!): String!
}
